<?xml version="1.0" encoding="utf-8"?>
<!-- Shared bottom navigation bar for registration step screens -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:gravity="center"
    android:paddingStart="@dimen/space_xl"
    android:paddingEnd="@dimen/space_xl"
    android:paddingTop="16dp"
    android:paddingBottom="32dp"
    android:background="@drawable/bg_bottom_nav">

    <!-- Next / Finish (primary CTA) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="12dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_nav_next"
            style="@style/Widget.Aura.Button.Primary.Gradient"
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:text="@string/nav_btn_next"
            android:textSize="16sp"
            app:icon="@android:drawable/ic_media_next"
            app:iconGravity="textEnd"
            app:iconPadding="8dp" />

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progress_loading"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminate="true"
            app:indicatorSize="24dp"
            app:trackThickness="3dp"
            app:indicatorColor="@color/colorOnPrimary"
            app:trackColor="@android:color/transparent"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Cancel / Back -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_nav_cancel"
        style="@style/Widget.Material3.Button.TextButton"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:text="@string/nav_btn_back"
        android:textColor="@color/colorPrimary"
        android:fontFamily="@font/manrope_bold"
        android:textSize="14sp" />

</LinearLayout>
